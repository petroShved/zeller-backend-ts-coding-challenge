{
  "name": "zeller-backend-ts-coding-challenge",
  "version": "1.0.0",
  "description": "Zeller is starting a computer store. You have been engaged to build the checkout system. We will start with the following products in our catalogue",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/petroShved/zeller-backend-ts-coding-challenge.git"
  },
  "engines": {
    "node": "16.14.2"
  },
  "devDependencies": {
    "typescript": "4.6.3"
  }
}
